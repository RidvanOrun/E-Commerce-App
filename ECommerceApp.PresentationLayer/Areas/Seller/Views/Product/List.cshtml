@model IEnumerable<ECommerceApp.DomainLayer.Entities.Concrete.Product>

@{
    ViewData["Title"] = "List";
    Layout = "~/Areas/Seller/Views/Shared/_Layout.cshtml";
}

<div class="row mt-2">
    <div class="col-sm-12">
        <div class="card">
            <div class="card-header">
                <div class="card-title">List of Product</div>

            </div>
            <div class="card-body">

                <div class="form-group">

                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <td>Id No</td>
                                <th>Name</th>
                                <th>Category Id</th>
                                <th>Description</th>
                                <th>Desc Text</th>
                                <th>Seller</th>
                                <th>UnitPrice</th>
                                <th>Details</th>
                                <th>Edit</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.Id</td>
                                    <td>@item.ProductName</td>
                                    <td>@item.CategoryId</td>
                                    <td>@item.Description</td>
                                    <td>@item.DescText</td>
                                    <td>@item.AppUserToProducts.</td>
                                    <td>@item.UnitPrice.ToString("C2")</td>

                                    <td>
                                        <a asp-area="" asp-controller="Product" asp-action="Details" asp-route-id="@item.Id" class="btn btn-info">Details</a>
                                    </td>
                                    <td>
                                        <a asp-area="Seller" asp-controller="Product" asp-action="Update" asp-route-id="@item.Id" class="btn btn-info">Edit</a>
                                    </td>

                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

</div>